#!/usr/bin/env bash
# disasm-codemode wrapper -> skills/bn-hunt/scripts/disasm-range.py  (auto-on-PATH when the plugin is installed)
root="${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$(readlink -f "$0")")/.." && pwd)}"
exec python3 "$root/skills/bn-hunt/scripts/disasm-range.py" "$@"
