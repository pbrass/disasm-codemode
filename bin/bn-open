#!/usr/bin/env bash
# disasm-codemode wrapper -> open a binary as a tab in the running BN GUI
root="${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$(readlink -f "$0")")/.." && pwd)}"
exec bash "$root/skills/binary-ninja/scripts/bnopen.sh" "$@"
