#!/usr/bin/env bash
# disasm-codemode wrapper -> skills/symbolicate/scripts/extract.py
root="${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$(readlink -f "$0")")/.." && pwd)}"
exec python3 "$root/skills/symbolicate/scripts/extract.py" "$@"
